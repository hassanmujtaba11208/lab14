// C++ program to add two number using
// addition operator
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 50, b = 20;

    // Adding the two numbers and printing
    // their sum
    cout << a + b;
    return 0;
}
